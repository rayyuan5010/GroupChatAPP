part of stiker_message_widget;

class _StikerMessageDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('stiker_message_desktop'),
    );
  }
}