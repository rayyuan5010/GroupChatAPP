part of text_message_widget;

class _TextMessageTablet extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('text_message_tablet'),
    );
  }
}