part of image_message_widget;

class _ImageMessageTablet extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('image_message_tablet'),
    );
  }
}