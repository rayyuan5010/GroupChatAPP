part of image_message_widget;

class _ImageMessageDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('image_message_desktop'),
    );
  }
}