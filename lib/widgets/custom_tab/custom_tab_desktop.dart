part of custom_tab_widget;

class _CustomTabDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('custom_tab_desktop'),
    );
  }
}