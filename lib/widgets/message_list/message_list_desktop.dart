part of message_list_widget;

class _MessageListDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('message_list_desktop'),
    );
  }
}